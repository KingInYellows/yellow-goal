# fixture-python-app

Fixture repository: a minimal Python application, used to exercise the `python-app` detector
class (pyproject.toml manifest, `src/` layout, pytest tests).

## Commands

```
pip install -e .[dev]
pytest
```
