[project]
name = "fixture-python-app"
version = "0.1.0"
description = "Fixture: a small Python application with a pyproject.toml and a tests directory."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
