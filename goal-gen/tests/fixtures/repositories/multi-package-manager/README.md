# fixture-multi-package-manager

Fixture repository: `package.json`, `pyproject.toml`, and `go.mod` all present at the same root.
Used to exercise multi-runtime/multi-package-manager detection (a repository must not assume a
single language or package manager).
