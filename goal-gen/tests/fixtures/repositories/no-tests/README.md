# fixture-no-tests

Fixture repository: application code with no `tests/`, `test/`, or `*.test.*` files anywhere, and
no `test` script beyond the package.json default. Used to exercise `missing_evidence` findings for
test-surface detection.
