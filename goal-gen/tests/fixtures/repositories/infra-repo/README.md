# fixture-infra-repo

Fixture repository: minimal Terraform + Ansible-ish content, used to exercise the `infra-repo`
detector class (infrastructure-as-code manifests, no application package manager).
