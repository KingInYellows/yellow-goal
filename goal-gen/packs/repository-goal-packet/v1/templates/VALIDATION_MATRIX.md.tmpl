# Validation Matrix

Working copy for the implementation and verification waves. Every row starts `NOT RUN`. Update
`Result` to `passed` / `failed` / `skipped` / `unavailable` as each criterion is verified.
Never mark a criterion `passed` without evidence in the `Evidence` column.

| Criterion | Method | Environment | Result | Evidence |
|---|---|---|---|---|
{{VALIDATION_MATRIX_ROWS}}
