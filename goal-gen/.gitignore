# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/

# Env & secrets — secrets via env only (CLAUDE.md, ADR-0011). Never commit real values.
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Runtime data & per-run agent workspaces (self-hosted; never commit these)
/data/
/worktrees/
*.sqlite

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# ruvector vector storage (per-developer)
.ruvector/
ruvector.db

# Local agent / tooling state (per-developer; not shared)
.claude/agent-memory/
.claude/composio-usage.json
.claude/*.local.md
mempalace.yaml
entities.json
